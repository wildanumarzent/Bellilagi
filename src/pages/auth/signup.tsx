import Signup from '@/components/PageGroups/auth/Signup';
import { getLayout } from '@/layouts';

const Signupnpage = () => {
  return <Signup />;
};

Signupnpage.getLayout = getLayout;

export default Signupnpage;
